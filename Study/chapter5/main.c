#include <stdio.h>

int main()
{
	/* 5-1
	int nT;
	printf("키를 입력하세요 : ");
	scanf_s("%d", &nT);

	const int nPass = 150;
	if (nT > nPass)
		printf("결과 : 합격");
	else
		printf("결과 : 불합격");
	*/

	/* 5-2
	int a, b, c;
	int nMax = 0;

	scanf_s("%d%d%d", &a, &b, &c);

	if (a > b)
		nMax = a;
	else
		nMax = b;
	if (nMax < c)
		nMax = c;
		
	printf("Max : %d", nMax);
	*/

	return 0;
}